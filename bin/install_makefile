#!/bin/bash

cd "$(dirname "$0")"
MYSCRIPT_DIR="$(pwd -P)"
cd "${OLDPWD}"

if [ ! -d ".d42Makefile" ]; then
	mkdir ".d42Makefile"
fi
 
cp "${MYSCRIPT_DIR}"/.Makefile/gMakefile .d42Makefile/genMakefile

